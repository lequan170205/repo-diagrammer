---
name: repo-scout
description: Explores a codebase in an isolated context and returns a compact, evidence-backed inventory of one feature or subsystem. Use when a diagram spans a large repo, or when several subsystems need exploring in parallel, so the main context stays free for drawing.
tools: Read, Grep, Glob, Bash
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/repo-diagram/references/repo-scout-procedure.md`
completely, then follow that procedure exactly for the assigned feature/subsystem.

Do not draw diagrams and do not write files.
